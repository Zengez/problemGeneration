TITLE: Intermediate Value Theorem Applications (easy)
TAGS: Limits, Continuity, Intermediate Value Theorem, easy

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def intermediateValueApplication3Easy():
    fvec = [x^2, x^4, x^6]
    pick = RandInt(0,2)
    center = RandInt(-5,5)
    f = NonZeroInt(-5,5)*fvec[pick](x=(x-center)) + RandInt(-10,10)
    left = RandInt(center-6,center-1)
    right = RandInt(center+2,center+7)
    min = min(f(x=left),f(x=right),f(x=center))
    max = max(f(x=left),f(x=right),f(x=center))
    target = RandInt(min-10,max+10)
    if (target<max and target>min): ans=1
    else: ans=0
    return [f, left, right, target, ans]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def intermediateValueApplication3Easy():
    fvec = [x^2, x^4, x^6]
    pick = RandInt(0,2)
    center = RandInt(-5,5)
    f = NonZeroInt(-5,5)*fvec[pick](x=(x-center)) + RandInt(-10,10)
    left = RandInt(center-6,center-1)
    right = RandInt(center+2,center+7)
    min = min(f(x=left),f(x=right),f(x=center))
    max = max(f(x=left),f(x=right),f(x=center))
    target = RandInt(min-10,max+10)
    if (target<max and target>min): ans=1
    else: ans=0
    return [f, left, right, target, ans]

##### Problem p2
p2gen = intermediateValueApplication3Easy()
p2f = p2gen[0]
p2left = p2gen[1]
p2right = p2gen[2]
p2target = p2gen[3]
p2ans = p2gen[4]
\end{sagesilent}

\begin{problem}Let $g(x)=\sage{p2f}$. Use IVT to determine whether there exists $x\in[\sage{p2left},\sage{p2right}]$ such that $g(x)=\sage{p2target}$. Enter 1 if yes, 0 if no: $\answer{\sage{p2ans}}$.\end{problem}
