TITLE: Evaluating Limits with Limit Laws
TAGS: Limits, Limit Laws

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def limitLawOne4():
    var('x,y,z,X,Y,Z')
    c1=RandInt(-10,10); c2=RandInt(-10,10); c3=RandInt(-10,10); c4=RandInt(-10,10); target=RandInt(-2,2); f1=c1*x^3+c2*x^2+c3*x+c4; ans=f1(x=target)
    return [target, f1, ans]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def limitLawOne4():
    var('x,y,z,X,Y,Z')
    c1=RandInt(-10,10); c2=RandInt(-10,10); c3=RandInt(-10,10); c4=RandInt(-10,10); target=RandInt(-2,2); f1=c1*x^3+c2*x^2+c3*x+c4; ans=f1(x=target)
    return [target, f1, ans]

##### Problem p4
p4gen = limitLawOne4()
p4target = p4gen[0]
p4f1 = p4gen[1]
p4ans = p4gen[2]
\end{sagesilent}

\begin{problem}Calculate $\lim_{x\to\sage{p4target}}\sage{p4f1}=\answer{\sage{p4ans}}$.\end{problem}
