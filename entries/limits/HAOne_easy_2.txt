TITLE: Horizontal Asymptotes (easy)
TAGS: Limits, Asymptotes, Horizontal Asymptotes, easy

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def hAOneEasy():
    var('x,y,z,X,Y,Z')
    c1 = NonZeroInt(-5,5)
    c2 = NonZeroInt(-5,5)
    c3 = RandInt(-10,10)
    c4 = RandInt(-10,10)
    baseT = RandInt(2,10)
    basevec = [baseT,e]
    basepick = RandInt(0,1)
    base = basevec[basepick]
    f1 = c1*base^(c2*x - c3) + c4
    if c2 > 0:
        leftHA = c4; rightHA = LatexExpr(r"DNE")
    if c2 < 0:
        leftHA = LatexExpr(r"DNE"); rightHA = c4
    return [f1, leftHA, rightHA]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def hAOneEasy():
    var('x,y,z,X,Y,Z')
    c1 = NonZeroInt(-5,5)
    c2 = NonZeroInt(-5,5)
    c3 = RandInt(-10,10)
    c4 = RandInt(-10,10)
    baseT = RandInt(2,10)
    basevec = [baseT,e]
    basepick = RandInt(0,1)
    base = basevec[basepick]
    f1 = c1*base^(c2*x - c3) + c4
    if c2 > 0:
        leftHA = c4; rightHA = LatexExpr(r"DNE")
    if c2 < 0:
        leftHA = LatexExpr(r"DNE"); rightHA = c4
    return [f1, leftHA, rightHA]

##### Problem p2
p2gen = hAOneEasy()
p2f1 = p2gen[0]
p2leftHA = p2gen[1]
p2rightHA = p2gen[2]
\end{sagesilent}

\begin{problem}Determine the horizontal asymptotes of $g(x)=\sage{p2f1}$. Left: $\answer{\sage{p2leftHA}}$, Right: $\answer{\sage{p2rightHA}}$.\end{problem}
