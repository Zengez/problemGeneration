TITLE: One-Sided Limits
TAGS: Limits, One-Sided Limits

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def oneSidedLimits():
    var('x,y,z,X,Y,Z')
    c1 = NonZeroInt(-5,5)
    c2 = RandInt(-5,5)
    c3 = NonZeroInt(-5,5)
    c4 = RandInt(-5,5)
    c5 = NonZeroInt(-5,5)
    c6 = RandInt(-5,5)
    c7 = NonZeroInt(-5,5,[c2/c1,c4/c3,c6/c5])
    f1 = c1*x - c2
    f2 = c3*x - c4
    f3 = c5*x - c6
    f4 = x - c7
    fdisptop = expand((f1*f2))
    fdispbot = expand((f3*f4))
    ansf = f1*f2/f3
    ans1 = ansf(x=c7)*f4(x=(c7+1))*infinity
    ans2 = ansf(x=c7)*f4(x=(c7-1))*infinity
    return [fdisptop, fdispbot, c7, ans1, ans2]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def oneSidedLimits():
    var('x,y,z,X,Y,Z')
    c1 = NonZeroInt(-5,5)
    c2 = RandInt(-5,5)
    c3 = NonZeroInt(-5,5)
    c4 = RandInt(-5,5)
    c5 = NonZeroInt(-5,5)
    c6 = RandInt(-5,5)
    c7 = NonZeroInt(-5,5,[c2/c1,c4/c3,c6/c5])
    f1 = c1*x - c2
    f2 = c3*x - c4
    f3 = c5*x - c6
    f4 = x - c7
    fdisptop = expand((f1*f2))
    fdispbot = expand((f3*f4))
    ansf = f1*f2/f3
    ans1 = ansf(x=c7)*f4(x=(c7+1))*infinity
    ans2 = ansf(x=c7)*f4(x=(c7-1))*infinity
    return [fdisptop, fdispbot, c7, ans1, ans2]

##### Problem p1
p1gen = oneSidedLimits()
p1fdisptop = p1gen[0]
p1fdispbot = p1gen[1]
p1c7 = p1gen[2]
p1ans1 = p1gen[3]
p1ans2 = p1gen[4]
\end{sagesilent}

\begin{problem}
Compute the one-sided limits of $\frac{\sage{p1fdisptop}}{\sage{p1fdispbot}}$ as $x$ approaches $\sage{p1c7}$: $\lim_{x\to\sage{p1c7}^+}=\answer{\sage{p1ans1}}$, $\lim_{x\to\sage{p1c7}^-}=\answer{\sage{p1ans2}}$.
\end{problem}
