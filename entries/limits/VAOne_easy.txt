TITLE: Vertical Asymptotes (easy)
TAGS: Limits, Asymptotes, Vertical Asymptotes, easy

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def vAOneEasy():
    var('x,y,z,X,Y,Z')
    target = RandInt(-10,10)
    c1 = NonZeroInt(1,10,[0,target])
    c2 = NonZeroInt(1,10,[0,c1,target])
    c3 = NonZeroInt(1,10,[0,c1,c2,target])
    c4 = NonZeroInt(1,10,[0,c1,c2,c3,target])
    c5 = NonZeroInt(-30,30)
    c6 = NonZeroInt(-30,30)
    n1 = RandInt(0,4); n2 = RandInt(0,4); n3 = RandInt(0,4); n4 = RandInt(0,4); n5 = RandInt(0,4); n6 = RandInt(0,4)
    f1top = c5*(x-c1)^n1*(x-c2)^n2*(x-target)^n3
    f1bot = c6*(x-c3)^n4*(x-c4)^n5*(x-target)^n6
    ans = c3+c4
    if n6 > n3:
        ans = ans+target
    return [f1top, f1bot, ans]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def vAOneEasy():
    var('x,y,z,X,Y,Z')
    target = RandInt(-10,10)
    c1 = NonZeroInt(1,10,[0,target])
    c2 = NonZeroInt(1,10,[0,c1,target])
    c3 = NonZeroInt(1,10,[0,c1,c2,target])
    c4 = NonZeroInt(1,10,[0,c1,c2,c3,target])
    c5 = NonZeroInt(-30,30)
    c6 = NonZeroInt(-30,30)
    n1 = RandInt(0,4); n2 = RandInt(0,4); n3 = RandInt(0,4); n4 = RandInt(0,4); n5 = RandInt(0,4); n6 = RandInt(0,4)
    f1top = c5*(x-c1)^n1*(x-c2)^n2*(x-target)^n3
    f1bot = c6*(x-c3)^n4*(x-c4)^n5*(x-target)^n6
    ans = c3+c4
    if n6 > n3:
        ans = ans+target
    return [f1top, f1bot, ans]

##### Problem p1
p1gen = vAOneEasy()
p1f1top = p1gen[0]
p1f1bot = p1gen[1]
p1ans = p1gen[2]
\end{sagesilent}

\begin{problem}Determine vertical asymptotes for $f(x)=\frac{\sage{p1f1top}}{\sage{p1f1bot}}$. Sum of $x$ values: $\answer{\sage{p1ans}}$.\end{problem}
