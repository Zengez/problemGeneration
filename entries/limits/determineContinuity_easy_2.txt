TITLE: Determine Continuity (easy)
TAGS: Continuity, Limits, easy

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def determineContinuityEasy():
    var('x,y,z,X,Y,Z')
    c1 = RandInt(-3,3); c2 = RandInt(-3,3); c3 = RandInt(-3,3); c4 = RandInt(-3,3); c5 = RandInt(-3,3); c6 = RandInt(-3,3)
    fTop = expand((x-c1)*(x-c2)*(x-c3))
    fBot = expand((x-c4)*(x-c5)*(x-c6))
    target = choice([c4,c5,c6,NonZeroInt(-3,3,[c4,c5,c6]),NonZeroInt(-3,3,[c4,c5,c6])])
    if fBot(x=target)==0:
        ans=2
    else:
        ans=1
    return [fTop, fBot, target, ans]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def determineContinuityEasy():
    var('x,y,z,X,Y,Z')
    c1 = RandInt(-3,3); c2 = RandInt(-3,3); c3 = RandInt(-3,3); c4 = RandInt(-3,3); c5 = RandInt(-3,3); c6 = RandInt(-3,3)
    fTop = expand((x-c1)*(x-c2)*(x-c3))
    fBot = expand((x-c4)*(x-c5)*(x-c6))
    target = choice([c4,c5,c6,NonZeroInt(-3,3,[c4,c5,c6]),NonZeroInt(-3,3,[c4,c5,c6])])
    if fBot(x=target)==0:
        ans=2
    else:
        ans=1
    return [fTop, fBot, target, ans]

##### Problem p2
p2gen = determineContinuityEasy()
p2fTop = p2gen[0]
p2fBot = p2gen[1]
p2target = p2gen[2]
p2ans = p2gen[3]
\end{sagesilent}

\begin{problem}Let $r(x)=\frac{\sage{p2fTop}}{\sage{p2fBot}}$. Is $r$ continuous at $x=\sage{p2target}$? Enter 1 for continuous, 2 for not continuous: $\answer{\sage{p2ans}}$.\end{problem}
