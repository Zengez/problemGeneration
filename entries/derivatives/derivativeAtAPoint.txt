TITLE: Derivative at a Point
TAGS: Derivatives

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def derivativeAtAPoint():
    var('x,y,z,X,Y,Z')
    funcvec = [x, x^2, sqrt(x)]
    c1 = NonZeroInt(-5,5)
    c2 = NonZeroInt(-5,5)
    c3 = RandInt(-5,5)
    c4 = RandInt(-5,5)
    ansImage = 0
    select = RandInt(0,2)
    f1 = c1*funcvec[select](x=(c2*x-c3)) + c4
    target = NonZeroInt(-5,5,[c3/c2])
    f1der = derivative(f1,x)
    ans = f1der(x=target)
    track = 0
    ansImage = imag(ans)
    while abs(ansImage) > 0:
        c1 = NonZeroInt(-5,5)
        c2 = NonZeroInt(-5,5)
        c3 = RandInt(-5,5)
        c4 = RandInt(-5,5)
        target = NonZeroInt(-5,5,[c3/c2])
        f1 = c1*funcvec[select](x=(c2*x-c3)) + c4
        f1der = derivative(f1,x)
        ans = f1der(x=target)
        ansImage = imag(ans)
    return [f1, target, ans]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def derivativeAtAPoint():
    var('x,y,z,X,Y,Z')
    funcvec = [x, x^2, sqrt(x)]
    c1 = NonZeroInt(-5,5)
    c2 = NonZeroInt(-5,5)
    c3 = RandInt(-5,5)
    c4 = RandInt(-5,5)
    ansImage = 0
    select = RandInt(0,2)
    f1 = c1*funcvec[select](x=(c2*x-c3)) + c4
    target = NonZeroInt(-5,5,[c3/c2])
    f1der = derivative(f1,x)
    ans = f1der(x=target)
    track = 0
    ansImage = imag(ans)
    while abs(ansImage) > 0:
        c1 = NonZeroInt(-5,5)
        c2 = NonZeroInt(-5,5)
        c3 = RandInt(-5,5)
        c4 = RandInt(-5,5)
        target = NonZeroInt(-5,5,[c3/c2])
        f1 = c1*funcvec[select](x=(c2*x-c3)) + c4
        f1der = derivative(f1,x)
        ans = f1der(x=target)
        ansImage = imag(ans)
    return [f1, target, ans]

##### Problem p1
p1gen = derivativeAtAPoint()
p1f1 = p1gen[0]
p1target = p1gen[1]
p1ans = p1gen[2]
\end{sagesilent}

\begin{problem}Compute the derivative of $f(x)=\sage{p1f1}$ at $x=\sage{p1target}$. $f'(\sage{p1target})=\answer{\sage{p1ans}}$\end{problem}
