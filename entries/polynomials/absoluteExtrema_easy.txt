TITLE: Absolute Extrema (easy)
TAGS: Polynomials, Extrema, Functions, easy

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def absoluteExtremaEasy():
    var('x')
    c1 = NonZeroInt(-10,10)
    c2 = NonZeroInt(-10,10,[0,c1])
    c3 = NonZeroInt(-10,10,[0,c1,c2])
    c4 = NonZeroInt(-10,10,[0,c1,c2,c3])
    pwr1 = RandInt(1,20)
    pwr2 = RandInt(1,20)
    pwr3 = RandInt(1,20)
    pwr4 = RandInt(1,20)
    f1 = c1*x^pwr1
    f2 = c2*x^pwr2
    f3 = c3*x^pwr3
    f4 = c4*x^pwr4
    Disp = f1+f2+f3+f4
    deg = Disp.degree(x)
    if deg/2== floor(deg/2):
        ans = (1+sign(Disp.coefficient(x^deg)))/2 + 1
    else:
        ans = 0
    return [Disp, ans]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def absoluteExtremaEasy():
    var('x')
    c1 = NonZeroInt(-10,10)
    c2 = NonZeroInt(-10,10,[0,c1])
    c3 = NonZeroInt(-10,10,[0,c1,c2])
    c4 = NonZeroInt(-10,10,[0,c1,c2,c3])
    pwr1 = RandInt(1,20)
    pwr2 = RandInt(1,20)
    pwr3 = RandInt(1,20)
    pwr4 = RandInt(1,20)
    f1 = c1*x^pwr1
    f2 = c2*x^pwr2
    f3 = c3*x^pwr3
    f4 = c4*x^pwr4
    Disp = f1+f2+f3+f4
    deg = Disp.degree(x)
    if deg/2== floor(deg/2):
        ans = (1+sign(Disp.coefficient(x^deg)))/2 + 1
    else:
        ans = 0
    return [Disp, ans]

##### Problem p1
p1gen = absoluteExtremaEasy()
p1Disp = p1gen[0]
p1ans = p1gen[1]
\end{sagesilent}

\begin{problem}
\begin{hint}Remember that the absolute extrema depends on the degree and leading coefficient for a polynomial.\end{hint}
Consider the function $f(x) = \sage{p1Disp}$. Then this function has:
\begin{enumerate}\item[0] No Absolute Extrema\item[1] An Absolute Maximum\item[2] An Absolute Minimum\end{enumerate}
Enter the number that corresponds to your answer: $\answer{\sage{p1ans}}$
\end{problem}
