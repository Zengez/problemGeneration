TITLE: Complex Numbers (medium)
TAGS: Polynomials, Complex Numbers, Functions, medium

EXAMPLE:


CODE-SAGE:
def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def complexNumbersMedium():
    c1 = NonZeroInt(-10,10)
    c2 = NonZeroInt(-10,10)
    c3 = NonZeroInt(-10,10)
    c4 = NonZeroInt(-10,10)

    f1 = c1 + c2*i
    f2 = c3 + c4*i

    ans = f1*f2
    ansR=ans.real()
    ansI=ans.imag()
    return [f1, f2, ansR, ansI]

CODE-XIMERA:
\begin{sagesilent}
def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def complexNumbersMedium():
    c1 = NonZeroInt(-10,10)
    c2 = NonZeroInt(-10,10)
    c3 = NonZeroInt(-10,10)
    c4 = NonZeroInt(-10,10)

    f1 = c1 + c2*i
    f2 = c3 + c4*i

    ans = f1*f2
    ansR=ans.real()
    ansI=ans.imag()
    return [f1, f2, ansR, ansI]

##### Problem p2
p2gen = complexNumbersMedium()
p2f1 = p2gen[0]
p2f2 = p2gen[1]
p2ansR = p2gen[2]
p2ansI = p2gen[3]
\end{sagesilent}

\begin{problem}
            \begin{hint}
                When multiplying complex numbers, you want to distribute out the terms, like you are multiplying two binomials.
            \end{hint}
            \begin{hint}
                Don't forget that $i^2=-1$, so the two imaginary parts being multiplied together give you a real number!
            \end{hint}
            Simplify the following into $A + B\cdot i$ form: $(\sage{p2f1})(\sage{p2f2}) = \answer{\sage{p2ansR}} + \answer{\sage{p2ansI}}\cdot i$
        \end{problem}
