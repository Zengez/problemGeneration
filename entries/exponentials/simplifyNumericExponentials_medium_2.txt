TITLE: Simplifying Numeric Exponentials (medium)
TAGS: Exponential Functions, Simplifying, Arithmetic, medium

EXAMPLE:
\\begin{problem}Compute: $(3)^{-1}=\frac{1}{3}$; $(-2)^{-4}=\frac{1}{16}$; $(6)^{4}=1296$.\\end{problem}

CODE-SAGE:
def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def simplifyNumericExponentialsMedium():
    ans1=99999/5
    while ((abs(numerator(ans1))>2000) or abs((denominator(ans1))>2000)): b1=NonZeroInt(-8,8,[-1,0,1]);pwr1=NonZeroInt(-6,6,[0,1]);ans1=b1^pwr1
    ans2=99999/5
    while ((abs(numerator(ans2))>2000) or abs((denominator(ans2))>2000)): b2=NonZeroInt(-8,8,[-1,0,1,b1]);pwr2=NonZeroInt(-6,6,[0,1]);ans2=b2^pwr2
    ans3=99999/5
    while ((abs(numerator(ans3))>2000) or abs((denominator(ans3))>2000)): b3=NonZeroInt(-8,8,[-1,0,1,b1,b2]);pwr3=NonZeroInt(-6,6,[0,1]);ans3=b3^pwr3
    return [b1, pwr1, b2, pwr2, b3, pwr3, ans1, ans2, ans3]

CODE-XIMERA:
\begin{sagesilent}
def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def simplifyNumericExponentialsMedium():
    ans1=99999/5
    while ((abs(numerator(ans1))>2000) or abs((denominator(ans1))>2000)): b1=NonZeroInt(-8,8,[-1,0,1]);pwr1=NonZeroInt(-6,6,[0,1]);ans1=b1^pwr1
    ans2=99999/5
    while ((abs(numerator(ans2))>2000) or abs((denominator(ans2))>2000)): b2=NonZeroInt(-8,8,[-1,0,1,b1]);pwr2=NonZeroInt(-6,6,[0,1]);ans2=b2^pwr2
    ans3=99999/5
    while ((abs(numerator(ans3))>2000) or abs((denominator(ans3))>2000)): b3=NonZeroInt(-8,8,[-1,0,1,b1,b2]);pwr3=NonZeroInt(-6,6,[0,1]);ans3=b3^pwr3
    return [b1, pwr1, b2, pwr2, b3, pwr3, ans1, ans2, ans3]

##### Problem p2
p2gen = simplifyNumericExponentialsMedium()
p2b1 = p2gen[0]
p2pwr1 = p2gen[1]
p2b2 = p2gen[2]
p2pwr2 = p2gen[3]
p2b3 = p2gen[4]
p2pwr3 = p2gen[5]
p2ans1 = p2gen[6]
p2ans2 = p2gen[7]
p2ans3 = p2gen[8]
\end{sagesilent}

\begin{problem}Compute $\sage{p2b1}^{\sage{p2pwr1}},\sage{p2b2}^{\sage{p2pwr2}},\sage{p2b3}^{\sage{p2pwr3}}$: $\answer{\sage{p2ans1}},\answer{\sage{p2ans2}},\answer{\sage{p2ans3}}$.\end{problem}
