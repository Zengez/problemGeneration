TITLE: Solving Exponential Equations (hard)
TAGS: Exponential Functions, Solving Equations, hard

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingExponentialEquationsHard():
    b1=RandInt(2,7);b2=5;b3=5
    while ((b2==b3) or (b2==b3^(-1)) or ((b2+b3)>800) or ((b2^(-1)+b3^(-1))>800)): pwr1=NonZeroInt(-5,5,[-1,0,1]);pwr2=NonZeroInt(-4,4,[-1,0,1]);b2=b1^pwr1;b3=b1^pwr2
    c1=NonZeroInt(-5,5);c2=RandInt(-5,5);c3=NonZeroInt(-5,5,[c1*pwr1/pwr2]);c4=RandInt(-5,5);f1=c1*x+c2;f2=c3*x+c4;ans1=(c4*pwr2-c2*pwr1)/(c1*pwr1-c3*pwr2)
    return [b2, f1, b3, f2, ans1]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingExponentialEquationsHard():
    b1=RandInt(2,7);b2=5;b3=5
    while ((b2==b3) or (b2==b3^(-1)) or ((b2+b3)>800) or ((b2^(-1)+b3^(-1))>800)): pwr1=NonZeroInt(-5,5,[-1,0,1]);pwr2=NonZeroInt(-4,4,[-1,0,1]);b2=b1^pwr1;b3=b1^pwr2
    c1=NonZeroInt(-5,5);c2=RandInt(-5,5);c3=NonZeroInt(-5,5,[c1*pwr1/pwr2]);c4=RandInt(-5,5);f1=c1*x+c2;f2=c3*x+c4;ans1=(c4*pwr2-c2*pwr1)/(c1*pwr1-c3*pwr2)
    return [b2, f1, b3, f2, ans1]

##### Problem p3
p3gen = solvingExponentialEquationsHard()
p3b2 = p3gen[0]
p3f1 = p3gen[1]
p3b3 = p3gen[2]
p3f2 = p3gen[3]
p3ans1 = p3gen[4]
\end{sagesilent}

\begin{problem}$\sage{p3b2}^{\sage{p3f1}}=\sage{p3b3}^{\sage{p3f2}}$, $x=\answer{\sage{p3ans1}}$.\end{problem}
