TITLE: Solving Unsimplified Radicals (medium)
TAGS: Radical Functions, Solving Equations, medium

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingUnsimplifiedRadicalsMedium():
    A=1;B=1;D=1;d=1
    while A*D+B*A==2*A*d:
        A=NonZeroInt(-5,5);B=RandInt(-5,5);D=RandInt(-5,5);d=RandInt(-5,5)
    ans1=B/A;ans2=D/A;f2=A*x-d;f1=(A*(B+D)-2*A*d)*x+d^2-B*D;ans=LatexExpr(r"DNE");LHS1=-infinity;LHS2=-infinity
    if f1(x=ans1)>=0: LHS1=sqrt(f1(x=ans1))
    if f1(x=ans2)>=0: LHS2=sqrt(f1(x=ans2))
    if ((LHS1==f2(x=ans1)) or (LHS2==f2(x=ans2))):
        ans=0
        if LHS1==f2(x=ans1): ans=ans+ans1
        if ((LHS2==f2(x=ans2)) and (ans1!=ans2)): ans=ans+ans2
    return [f1, f2, ans]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingUnsimplifiedRadicalsMedium():
    A=1;B=1;D=1;d=1
    while A*D+B*A==2*A*d:
        A=NonZeroInt(-5,5);B=RandInt(-5,5);D=RandInt(-5,5);d=RandInt(-5,5)
    ans1=B/A;ans2=D/A;f2=A*x-d;f1=(A*(B+D)-2*A*d)*x+d^2-B*D;ans=LatexExpr(r"DNE");LHS1=-infinity;LHS2=-infinity
    if f1(x=ans1)>=0: LHS1=sqrt(f1(x=ans1))
    if f1(x=ans2)>=0: LHS2=sqrt(f1(x=ans2))
    if ((LHS1==f2(x=ans1)) or (LHS2==f2(x=ans2))):
        ans=0
        if LHS1==f2(x=ans1): ans=ans+ans1
        if ((LHS2==f2(x=ans2)) and (ans1!=ans2)): ans=ans+ans2
    return [f1, f2, ans]

##### Problem p2
p2gen = solvingUnsimplifiedRadicalsMedium()
p2f1 = p2gen[0]
p2f2 = p2gen[1]
p2ans = p2gen[2]
\end{sagesilent}

\begin{problem}Find the sum of real solutions to $\sqrt{\sage{p2f1}}=\sage{p2f2}$: $\answer{\sage{p2ans}}$.\end{problem}
