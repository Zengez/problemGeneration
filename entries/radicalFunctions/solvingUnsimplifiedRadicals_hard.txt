TITLE: Solving Unsimplified Radicals (hard)
TAGS: Radical Functions, Solving Equations, hard

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingUnsimplifiedRadicalsHard():
    ans1=100;ans2=200
    while ans1>50 or ans2>50:
        c1=NonZeroInt(1,5);c2=RandInt(1,5);c3=RandInt(1,5);px=c1*(x-c2)^2+c3;w=RandInt(1,3);h=px(x=(c2+w));wprime=RandInt(2,5);bprime=RandInt(c2+1,10);aprime=NonZeroInt(1,5,[c1]);cprime=h-aprime*wprime^2;gap=bprime+wprime-c2-w;left=sqrt((x-c3)/c1)+c2+gap;right=sqrt((x-cprime)/aprime)+bprime;d=c2+gap-bprime;e=c3*aprime-cprime*c1-d^2*c1*aprime;A=(c1-aprime)^2;B=2*(c1-aprime)*e-4*d^2*aprime^2*c1;C=e^2+4*d^2*aprime^2*c1*c3;ans1=(-B+sqrt(B^2-4*A*C))/(2*A);ans2=(-B-sqrt(B^2-4*A*C))/(2*A)
        if left(x=ans1)==right(x=ans1):
            if left(x=ans2)==right(x=ans2): ans=ans1+ans2
            else: ans=ans1
        else: ans=ans2
        if ans1==ans2:
            if left(x=ans1)==right(x=ans1): ans=ans1
            else: ans=LatexExpr(r"DNE")
    return [left, right, ans]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingUnsimplifiedRadicalsHard():
    ans1=100;ans2=200
    while ans1>50 or ans2>50:
        c1=NonZeroInt(1,5);c2=RandInt(1,5);c3=RandInt(1,5);px=c1*(x-c2)^2+c3;w=RandInt(1,3);h=px(x=(c2+w));wprime=RandInt(2,5);bprime=RandInt(c2+1,10);aprime=NonZeroInt(1,5,[c1]);cprime=h-aprime*wprime^2;gap=bprime+wprime-c2-w;left=sqrt((x-c3)/c1)+c2+gap;right=sqrt((x-cprime)/aprime)+bprime;d=c2+gap-bprime;e=c3*aprime-cprime*c1-d^2*c1*aprime;A=(c1-aprime)^2;B=2*(c1-aprime)*e-4*d^2*aprime^2*c1;C=e^2+4*d^2*aprime^2*c1*c3;ans1=(-B+sqrt(B^2-4*A*C))/(2*A);ans2=(-B-sqrt(B^2-4*A*C))/(2*A)
        if left(x=ans1)==right(x=ans1):
            if left(x=ans2)==right(x=ans2): ans=ans1+ans2
            else: ans=ans1
        else: ans=ans2
        if ans1==ans2:
            if left(x=ans1)==right(x=ans1): ans=ans1
            else: ans=LatexExpr(r"DNE")
    return [left, right, ans]

##### Problem p3
p3gen = solvingUnsimplifiedRadicalsHard()
p3left = p3gen[0]
p3right = p3gen[1]
p3ans = p3gen[2]
\end{sagesilent}

\begin{problem}Find the sum of real solutions to $\sage{p3left}=\sage{p3right}$: $\answer{\sage{p3ans}}$.\end{problem}
