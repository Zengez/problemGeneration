TITLE: Solving Unsimplified Radicals (easy)
TAGS: Radical Functions, Solving Equations, easy

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingUnsimplifiedRadicalsEasy():
    c1=RandInt(-5,5);c2=RandInt(-5,5);c3=NonZeroInt(-5,5);f1=c3*x+c1;ansT=(c2^2-c1)/c3
    if sqrt(f1(x=ansT))==c2: ans=ansT
    else: ans=LatexExpr(r'DNE')
    return [f1, c2, ans]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingUnsimplifiedRadicalsEasy():
    c1=RandInt(-5,5);c2=RandInt(-5,5);c3=NonZeroInt(-5,5);f1=c3*x+c1;ansT=(c2^2-c1)/c3
    if sqrt(f1(x=ansT))==c2: ans=ansT
    else: ans=LatexExpr(r'DNE')
    return [f1, c2, ans]

##### Problem p1
p1gen = solvingUnsimplifiedRadicalsEasy()
p1f1 = p1gen[0]
p1c2 = p1gen[1]
p1ans = p1gen[2]
\end{sagesilent}

\begin{problem}Solve $\sqrt{\sage{p1f1}}=\sage{p1c2}$: $\answer{\sage{p1ans}}$.\end{problem}
