TITLE: Computing Absolute Values (easy)
TAGS: Functions, Absolute Value, easy

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def computeAbsValEasy():
    ans = 500
    while abs(ans)>300:
        f1 = abs( NonZeroInt(-5,5)*x + RandInt(-10,10)) + NonZeroInt(-5,5)*x + RandInt(-10,10)
        target = RandInt(-5,5)
        ans = f1(x=target)
    return [f1, target, ans]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def computeAbsValEasy():
    ans = 500
    while abs(ans)>300:
        f1 = abs( NonZeroInt(-5,5)*x + RandInt(-10,10)) + NonZeroInt(-5,5)*x + RandInt(-10,10)
        target = RandInt(-5,5)
        ans = f1(x=target)
    return [f1, target, ans]

##### Problem p1
p1gen = computeAbsValEasy()
p1f1 = p1gen[0]
p1target = p1gen[1]
p1ans = p1gen[2]
\end{sagesilent}

\begin{problem}
\begin{hint}All we want is to compute a value, so it is ``simply'' (maybe not so simple in practice to actually compute it) a matter of plugging in the value and doing the arithmetic.\end{hint}
Consider the function $f(x) = \sage{p1f1}$. Compute $f(\sage{p1target}) = \answer{\sage{p1ans}}$.
\end{problem}
