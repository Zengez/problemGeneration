TITLE: Solving Absolute Value Equations (hard)
TAGS: Functions, Absolute Value, hard

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solveAbsValHard():
    c5 = 1
    c6 = 1
    while c5==c6:
        c1 = NonZeroInt(-5,5)
        c3 = 2*NonZeroInt(-3,3,[(c1-1)/2,c1/2]) + c1%2
        c5 = NonZeroInt(-5,5,[(c1+c3)/2])
        c7 = RandInt(-5,5)
        c4 = (2*c5*(-c5 + c7 + c3) + c1*(c1 - c3 - 2*c7))/(c3 - 2*c5 + c1)
        c2 = 2*c7 - c1 + c3 + c4
        c6 = (c1 + c2 - c5 - c7)
        c8 = (c3*c4 - c1*c2)/2
        fdisp1 = abs(expand( (x - c5)*(x - c6) ) )
        fdisp2 = c7*x + c8
        fsubfunc1 = (x - c1)*(x - c2)
        fsubfunc2 = (x - c3)*(x - c4)
        fcheck1 = expand( (x - c5)*(x - c6) ) - fdisp2
        fcheck2 = -(expand( -(x - c5)*(x - c6) ) - fdisp2)
        left = min(c5,c6)
        right = max(c5,c6)
        ansSum=0
        check=0
        if ((c1 <= left) or (right <= c1)):
            ansSum+=c1; check+=1
        if ((c2 <= left) or (right <= c2)):
            ansSum+=c2; check+=1
        if ((left < c3) and (c3 < right)):
            ansSum+=c3; check+=1
        if ((left < c4) and (c4 < right)):
            ansSum+=c4; check+=1
        if check==0:
            ansSum=LatexExpr(r'DNE')
    return [fdisp1, fdisp2, ansSum]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solveAbsValHard():
    c5 = 1
    c6 = 1
    while c5==c6:
        c1 = NonZeroInt(-5,5)
        c3 = 2*NonZeroInt(-3,3,[(c1-1)/2,c1/2]) + c1%2
        c5 = NonZeroInt(-5,5,[(c1+c3)/2])
        c7 = RandInt(-5,5)
        c4 = (2*c5*(-c5 + c7 + c3) + c1*(c1 - c3 - 2*c7))/(c3 - 2*c5 + c1)
        c2 = 2*c7 - c1 + c3 + c4
        c6 = (c1 + c2 - c5 - c7)
        c8 = (c3*c4 - c1*c2)/2
        fdisp1 = abs(expand( (x - c5)*(x - c6) ) )
        fdisp2 = c7*x + c8
        fsubfunc1 = (x - c1)*(x - c2)
        fsubfunc2 = (x - c3)*(x - c4)
        fcheck1 = expand( (x - c5)*(x - c6) ) - fdisp2
        fcheck2 = -(expand( -(x - c5)*(x - c6) ) - fdisp2)
        left = min(c5,c6)
        right = max(c5,c6)
        ansSum=0
        check=0
        if ((c1 <= left) or (right <= c1)):
            ansSum+=c1; check+=1
        if ((c2 <= left) or (right <= c2)):
            ansSum+=c2; check+=1
        if ((left < c3) and (c3 < right)):
            ansSum+=c3; check+=1
        if ((left < c4) and (c4 < right)):
            ansSum+=c4; check+=1
        if check==0:
            ansSum=LatexExpr(r'DNE')
    return [fdisp1, fdisp2, ansSum]

##### Problem p3
p3gen = solveAbsValHard()
p3fdisp1 = p3gen[0]
p3fdisp2 = p3gen[1]
p3ansSum = p3gen[2]
\end{sagesilent}

\begin{problem}Solve the equality $\sage{p3fdisp1}=\sage{p3fdisp2}$ for $x$. If there are no solutions, enter DNE. The sum of valid $x$-solutions is: $\answer{\sage{p3ansSum}}$\end{problem}
