TITLE: Evaluating Piecewise Functions (easy)
TAGS: Piecewise Functions, Evaluation, easy

EXAMPLE:


CODE-SAGE:
var('x')

def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def piecewiseComputationEasy():
    funcvec=[x,x^2,x^3,sqrt(abs(x)),ln(abs(x)+1),e^x]
    f1pick=RandInt(0,5);f2pick=RandInt(0,5);f1=NonZeroInt(-5,5)*funcvec[f1pick](x=(x-RandInt(-5,5)))+RandInt(-5,5);f2=NonZeroInt(-5,5)*funcvec[f2pick](x=(x-RandInt(-5,5)))+RandInt(-5,5);r1=RandInt(-15,15);eval=RandInt(r1-10,r1+10);ans=555
    while abs(ans)>250:
        r1=RandInt(-15,15);eval=RandInt(r1-10,r1+10)
        if eval<r1+1:ans=f1(x=eval)
        else:ans=f2(x=eval)
    return [f1, r1, f2, eval, ans]

CODE-XIMERA:
\begin{sagesilent}
var('x')
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def piecewiseComputationEasy():
    funcvec=[x,x^2,x^3,sqrt(abs(x)),ln(abs(x)+1),e^x]
    f1pick=RandInt(0,5);f2pick=RandInt(0,5);f1=NonZeroInt(-5,5)*funcvec[f1pick](x=(x-RandInt(-5,5)))+RandInt(-5,5);f2=NonZeroInt(-5,5)*funcvec[f2pick](x=(x-RandInt(-5,5)))+RandInt(-5,5);r1=RandInt(-15,15);eval=RandInt(r1-10,r1+10);ans=555
    while abs(ans)>250:
        r1=RandInt(-15,15);eval=RandInt(r1-10,r1+10)
        if eval<r1+1:ans=f1(x=eval)
        else:ans=f2(x=eval)
    return [f1, r1, f2, eval, ans]

##### Problem p1
p1gen = piecewiseComputationEasy()
p1f1 = p1gen[0]
p1r1 = p1gen[1]
p1f2 = p1gen[2]
p1eval = p1gen[3]
p1ans = p1gen[4]
\end{sagesilent}

\begin{problem}$f(x)=\begin{cases}\sage{p1f1}&x\leq\sage{p1r1}\\\sage{p1f2}&\sage{p1r1}<x\end{cases}$; evaluate $f(\sage{p1eval})=\answer{\sage{p1ans}}$.\end{problem}
