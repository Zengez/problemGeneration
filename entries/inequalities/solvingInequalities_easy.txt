TITLE: Solving Inequalities (easy)
TAGS: Functions, Inequalities, easy

EXAMPLE:


CODE-SAGE:
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingInequalitiesEasy():
    var('x,y,z,X,Y,Z,r,R')
    c1 = NonZeroInt(-5,5)
    c2 = RandInt(-5,5)
    c3 = NonZeroInt(-5,5,[c1])
    c4 = RandInt(-5,5)
    fLeft = c1*x-c2
    fRight = c3*x-c4
    f = fLeft-fRight
    zero = -f(x=0)/(f.coefficient(x))
    sym1Choice=RandInt(0,3)
    if sym1Choice==0:
        fDisp = (fLeft < fRight)
        if f.coefficient(x)>0:
            leftEnd = -infinity; rightEnd = zero
        else:
            leftEnd = zero; rightEnd = infinity
    if sym1Choice==1:
        fDisp = (fLeft <= fRight)
        if f.coefficient(x)>0:
            leftEnd = -infinity; rightEnd = zero
        else:
            leftEnd = zero; rightEnd = infinity
    if sym1Choice==2:
        fDisp = (fLeft > fRight)
        if f.coefficient(x)>0:
            leftEnd = zero; rightEnd = infinity
        else:
            leftEnd = -infinity; rightEnd = zero
    if sym1Choice==3:
        fDisp = (fLeft >= fRight)
        if f.coefficient(x)>0:
            leftEnd = zero; rightEnd = infinity
        else:
            leftEnd = -infinity; rightEnd = zero
    return [fDisp]

CODE-XIMERA:
\begin{sagesilent}
def RandInt(a,b):
    """ Returns a random integer in [`a`,`b`]. Note that `a` and `b` should be integers themselves to avoid unexpected behavior.
    """
    return QQ(randint(int(a),int(b)))
    # return choice(range(a,b+1))

def NonZeroInt(b,c, avoid = [0]):
    """ Returns a random integer in [`b`,`c`] which is not in `av`. 
        If `av` is not specified, defaults to a non-zero integer.
    """
    while True:
        a = RandInt(b,c)
        if a not in avoid:
            return a

def solvingInequalitiesEasy():
    var('x,y,z,X,Y,Z,r,R')
    c1 = NonZeroInt(-5,5)
    c2 = RandInt(-5,5)
    c3 = NonZeroInt(-5,5,[c1])
    c4 = RandInt(-5,5)
    fLeft = c1*x-c2
    fRight = c3*x-c4
    f = fLeft-fRight
    zero = -f(x=0)/(f.coefficient(x))
    sym1Choice=RandInt(0,3)
    if sym1Choice==0:
        fDisp = (fLeft < fRight)
        if f.coefficient(x)>0:
            leftEnd = -infinity; rightEnd = zero
        else:
            leftEnd = zero; rightEnd = infinity
    if sym1Choice==1:
        fDisp = (fLeft <= fRight)
        if f.coefficient(x)>0:
            leftEnd = -infinity; rightEnd = zero
        else:
            leftEnd = zero; rightEnd = infinity
    if sym1Choice==2:
        fDisp = (fLeft > fRight)
        if f.coefficient(x)>0:
            leftEnd = zero; rightEnd = infinity
        else:
            leftEnd = -infinity; rightEnd = zero
    if sym1Choice==3:
        fDisp = (fLeft >= fRight)
        if f.coefficient(x)>0:
            leftEnd = zero; rightEnd = infinity
        else:
            leftEnd = -infinity; rightEnd = zero
    return [fDisp]

##### Problem p1
p1gen = solvingInequalitiesEasy()
p1fDisp = p1gen[0]
\end{sagesilent}

\begin{problem}Find all solutions to the inequality: \[\sage{p1fDisp}\]\end{problem}
